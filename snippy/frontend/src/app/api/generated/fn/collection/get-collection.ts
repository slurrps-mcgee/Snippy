/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { CollectionResponse } from '../../models/collection-response';

export interface GetCollection$Params {
  shortId: string;
  q?: string;
}

export function getCollection(http: HttpClient, rootUrl: string, params: GetCollection$Params, context?: HttpContext): Observable<StrictHttpResponse<CollectionResponse>> {
  const rb = new RequestBuilder(rootUrl, getCollection.PATH, 'get');
  if (params) {
    rb.path('shortId', params.shortId, {});
    rb.query('q', params.q, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<CollectionResponse>;
    })
  );
}

getCollection.PATH = '/collections/{shortId}';

/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { CollectionListResponse } from '../../models/collection-list-response';

export interface GetUserCollections$Params {
  userName: string;
  page?: number;
  limit?: number;
  q?: string;
}

export function getUserCollections(http: HttpClient, rootUrl: string, params: GetUserCollections$Params, context?: HttpContext): Observable<StrictHttpResponse<CollectionListResponse>> {
  const rb = new RequestBuilder(rootUrl, getUserCollections.PATH, 'get');
  if (params) {
    rb.path('userName', params.userName, {});
    rb.query('page', params.page, {});
    rb.query('limit', params.limit, {});
    rb.query('q', params.q, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<CollectionListResponse>;
    })
  );
}

getUserCollections.PATH = '/collections/user/{userName}';

/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { CollectionResponse } from '../../models/collection-response';

export interface AddSnippetToCollection$Params {
  collectionId: string;
      body: {
'snippetId'?: string;
}
}

export function addSnippetToCollection(http: HttpClient, rootUrl: string, params: AddSnippetToCollection$Params, context?: HttpContext): Observable<StrictHttpResponse<CollectionResponse>> {
  const rb = new RequestBuilder(rootUrl, addSnippetToCollection.PATH, 'post');
  if (params) {
    rb.path('collectionId', params.collectionId, {});
    rb.body(params.body, 'application/json');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<CollectionResponse>;
    })
  );
}

addSnippetToCollection.PATH = '/collections/{collectionId}/snippets';

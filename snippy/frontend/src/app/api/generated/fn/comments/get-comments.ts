/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { CommentListResponse } from '../../models/comment-list-response';

export interface GetComments$Params {
  snippetId: string;
  page?: number;
  limit?: number;
  q?: string;
}

export function getComments(http: HttpClient, rootUrl: string, params: GetComments$Params, context?: HttpContext): Observable<StrictHttpResponse<CommentListResponse>> {
  const rb = new RequestBuilder(rootUrl, getComments.PATH, 'get');
  if (params) {
    rb.path('snippetId', params.snippetId, {});
    rb.query('page', params.page, {});
    rb.query('limit', params.limit, {});
    rb.query('q', params.q, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<CommentListResponse>;
    })
  );
}

getComments.PATH = '/comments/{snippetId}';

/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { FavoriteResponse } from '../../models/favorite-response';

export interface ToggleFavorite$Params {
  snippetId: string;
}

export function toggleFavorite(http: HttpClient, rootUrl: string, params: ToggleFavorite$Params, context?: HttpContext): Observable<StrictHttpResponse<FavoriteResponse>> {
  const rb = new RequestBuilder(rootUrl, toggleFavorite.PATH, 'post');
  if (params) {
    rb.path('snippetId', params.snippetId, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<FavoriteResponse>;
    })
  );
}

toggleFavorite.PATH = '/favorites/{snippetId}';

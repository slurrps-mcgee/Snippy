/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { FavoriteStatusResponse } from '../../models/favorite-status-response';

export interface GetFavoriteStatus$Params {
  snippetId: string;
}

export function getFavoriteStatus(http: HttpClient, rootUrl: string, params: GetFavoriteStatus$Params, context?: HttpContext): Observable<StrictHttpResponse<FavoriteStatusResponse>> {
  const rb = new RequestBuilder(rootUrl, getFavoriteStatus.PATH, 'get');
  if (params) {
    rb.path('snippetId', params.snippetId, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<FavoriteStatusResponse>;
    })
  );
}

getFavoriteStatus.PATH = '/favorites/{snippetId}';

/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { FollowResponse } from '../../models/follow-response';

export interface UnfollowUser$Params {
  userName: string;
}

export function unfollowUser(http: HttpClient, rootUrl: string, params: UnfollowUser$Params, context?: HttpContext): Observable<StrictHttpResponse<FollowResponse>> {
  const rb = new RequestBuilder(rootUrl, unfollowUser.PATH, 'delete');
  if (params) {
    rb.path('userName', params.userName, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<FollowResponse>;
    })
  );
}

unfollowUser.PATH = '/users/{userName}/follow';

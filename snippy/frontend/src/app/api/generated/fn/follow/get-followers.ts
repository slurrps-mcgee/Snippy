/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { UserListResponse } from '../../models/user-list-response';

export interface GetFollowers$Params {
  userName: string;
  page?: number;
  limit?: number;
  q?: string;
}

export function getFollowers(http: HttpClient, rootUrl: string, params: GetFollowers$Params, context?: HttpContext): Observable<StrictHttpResponse<UserListResponse>> {
  const rb = new RequestBuilder(rootUrl, getFollowers.PATH, 'get');
  if (params) {
    rb.path('userName', params.userName, {});
    rb.query('page', params.page, {});
    rb.query('limit', params.limit, {});
    rb.query('q', params.q, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<UserListResponse>;
    })
  );
}

getFollowers.PATH = '/users/{userName}/followers';

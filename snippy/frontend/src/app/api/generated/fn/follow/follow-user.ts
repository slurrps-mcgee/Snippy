/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { FollowResponse } from '../../models/follow-response';

export interface FollowUser$Params {
  userName: string;
}

export function followUser(http: HttpClient, rootUrl: string, params: FollowUser$Params, context?: HttpContext): Observable<StrictHttpResponse<FollowResponse>> {
  const rb = new RequestBuilder(rootUrl, followUser.PATH, 'post');
  if (params) {
    rb.path('userName', params.userName, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<FollowResponse>;
    })
  );
}

followUser.PATH = '/users/{userName}/follow';

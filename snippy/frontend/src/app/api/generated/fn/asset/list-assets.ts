/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { AssetListResponse } from '../../models/asset-list-response';

export interface ListAssets$Params {
  page?: number;
  limit?: number;
  q?: string;
}

export function listAssets(http: HttpClient, rootUrl: string, params?: ListAssets$Params, context?: HttpContext): Observable<StrictHttpResponse<AssetListResponse>> {
  const rb = new RequestBuilder(rootUrl, listAssets.PATH, 'get');
  if (params) {
    rb.query('page', params.page, {});
    rb.query('limit', params.limit, {});
    rb.query('q', params.q, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<AssetListResponse>;
    })
  );
}

listAssets.PATH = '/assets';

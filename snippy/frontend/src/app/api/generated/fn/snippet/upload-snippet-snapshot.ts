/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { SnippetResponse } from '../../models/snippet-response';

export interface UploadSnippetSnapshot$Params {
  snippetId: string;
      body: {
'file'?: Blob;
}
}

export function uploadSnippetSnapshot(http: HttpClient, rootUrl: string, params: UploadSnippetSnapshot$Params, context?: HttpContext): Observable<StrictHttpResponse<SnippetResponse>> {
  const rb = new RequestBuilder(rootUrl, uploadSnippetSnapshot.PATH, 'post');
  if (params) {
    rb.path('snippetId', params.snippetId, {});
    rb.body(params.body, 'multipart/form-data');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<SnippetResponse>;
    })
  );
}

uploadSnippetSnapshot.PATH = '/snippets/{snippetId}/snapshot';

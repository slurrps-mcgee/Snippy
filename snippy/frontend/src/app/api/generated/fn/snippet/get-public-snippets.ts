/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { SnippetListResponse } from '../../models/snippet-list-response';

export interface GetPublicSnippets$Params {
  page?: number;
  limit?: number;
  q?: string;
  sort?: 'newest' | 'views' | 'favorites' | 'forks';
  tag?: string;
}

export function getPublicSnippets(http: HttpClient, rootUrl: string, params?: GetPublicSnippets$Params, context?: HttpContext): Observable<StrictHttpResponse<SnippetListResponse>> {
  const rb = new RequestBuilder(rootUrl, getPublicSnippets.PATH, 'get');
  if (params) {
    rb.query('page', params.page, {});
    rb.query('limit', params.limit, {});
    rb.query('q', params.q, {});
    rb.query('sort', params.sort, {});
    rb.query('tag', params.tag, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<SnippetListResponse>;
    })
  );
}

getPublicSnippets.PATH = '/snippets/public';

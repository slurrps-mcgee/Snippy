/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { CreateSnippetRequest } from '../../models/create-snippet-request';
import { SnippetResponse } from '../../models/snippet-response';

export interface UpdateSnippet$Params {
  snippetId: string;
      body: CreateSnippetRequest
}

export function updateSnippet(http: HttpClient, rootUrl: string, params: UpdateSnippet$Params, context?: HttpContext): Observable<StrictHttpResponse<SnippetResponse>> {
  const rb = new RequestBuilder(rootUrl, updateSnippet.PATH, 'put');
  if (params) {
    rb.path('snippetId', params.snippetId, {});
    rb.body(params.body, 'application/json');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<SnippetResponse>;
    })
  );
}

updateSnippet.PATH = '/snippets/{snippetId}';

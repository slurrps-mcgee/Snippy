/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { SnippetResponse } from '../../models/snippet-response';

export interface ForkSnippet$Params {
  snippetId: string;
}

export function forkSnippet(http: HttpClient, rootUrl: string, params: ForkSnippet$Params, context?: HttpContext): Observable<StrictHttpResponse<SnippetResponse>> {
  const rb = new RequestBuilder(rootUrl, forkSnippet.PATH, 'post');
  if (params) {
    rb.path('snippetId', params.snippetId, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<SnippetResponse>;
    })
  );
}

forkSnippet.PATH = '/snippets/fork/{snippetId}';

/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { ViewCountResponse } from '../../models/view-count-response';

export interface RecordSnippetView$Params {
  snippetId: string;
}

export function recordSnippetView(http: HttpClient, rootUrl: string, params: RecordSnippetView$Params, context?: HttpContext): Observable<StrictHttpResponse<ViewCountResponse>> {
  const rb = new RequestBuilder(rootUrl, recordSnippetView.PATH, 'post');
  if (params) {
    rb.path('snippetId', params.snippetId, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<ViewCountResponse>;
    })
  );
}

recordSnippetView.PATH = '/snippets/{snippetId}/view';

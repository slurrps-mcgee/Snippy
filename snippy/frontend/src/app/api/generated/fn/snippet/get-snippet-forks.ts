/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { SnippetListResponse } from '../../models/snippet-list-response';

export interface GetSnippetForks$Params {
  shortId: string;
  page?: number;
  limit?: number;
  q?: string;
}

export function getSnippetForks(http: HttpClient, rootUrl: string, params: GetSnippetForks$Params, context?: HttpContext): Observable<StrictHttpResponse<SnippetListResponse>> {
  const rb = new RequestBuilder(rootUrl, getSnippetForks.PATH, 'get');
  if (params) {
    rb.path('shortId', params.shortId, {});
    rb.query('page', params.page, {});
    rb.query('limit', params.limit, {});
    rb.query('q', params.q, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<SnippetListResponse>;
    })
  );
}

getSnippetForks.PATH = '/snippets/{shortId}/forks';

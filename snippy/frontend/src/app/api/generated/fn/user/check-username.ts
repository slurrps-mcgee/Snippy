/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { UsernameAvailableResponse } from '../../models/username-available-response';

export interface CheckUsername$Params {
  userName: string;
}

export function checkUsername(http: HttpClient, rootUrl: string, params: CheckUsername$Params, context?: HttpContext): Observable<StrictHttpResponse<UsernameAvailableResponse>> {
  const rb = new RequestBuilder(rootUrl, checkUsername.PATH, 'get');
  if (params) {
    rb.path('userName', params.userName, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<UsernameAvailableResponse>;
    })
  );
}

checkUsername.PATH = '/users/check-username/{userName}';
